// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles

(function (modules, entry, mainEntry, parcelRequireName, globalName) {
  /* eslint-disable no-undef */
  var globalObject =
    typeof globalThis !== 'undefined'
      ? globalThis
      : typeof self !== 'undefined'
      ? self
      : typeof window !== 'undefined'
      ? window
      : typeof global !== 'undefined'
      ? global
      : {};
  /* eslint-enable no-undef */

  // Save the require from previous bundle to this closure if any
  var previousRequire =
    typeof globalObject[parcelRequireName] === 'function' &&
    globalObject[parcelRequireName];

  var cache = previousRequire.cache || {};
  // Do not use `require` to prevent Webpack from trying to bundle this call
  var nodeRequire =
    typeof module !== 'undefined' &&
    typeof module.require === 'function' &&
    module.require.bind(module);

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire =
          typeof globalObject[parcelRequireName] === 'function' &&
          globalObject[parcelRequireName];
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error("Cannot find module '" + name + "'");
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = (cache[name] = new newRequire.Module(name));

      modules[name][0].call(
        module.exports,
        localRequire,
        module,
        module.exports,
        this
      );
    }

    return cache[name].exports;

    function localRequire(x) {
      var res = localRequire.resolve(x);
      return res === false ? {} : newRequire(res);
    }

    function resolve(x) {
      var id = modules[name][1][x];
      return id != null ? id : x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [
      function (require, module) {
        module.exports = exports;
      },
      {},
    ];
  };

  Object.defineProperty(newRequire, 'root', {
    get: function () {
      return globalObject[parcelRequireName];
    },
  });

  globalObject[parcelRequireName] = newRequire;

  for (var i = 0; i < entry.length; i++) {
    newRequire(entry[i]);
  }

  if (mainEntry) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(mainEntry);

    // CommonJS
    if (typeof exports === 'object' && typeof module !== 'undefined') {
      module.exports = mainExports;

      // RequireJS
    } else if (typeof define === 'function' && define.amd) {
      define(function () {
        return mainExports;
      });

      // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }
})({"i44Yi":[function(require,module,exports) {
/*
  Do not manually edit this file, it was auto-generated by djelm
  https://github.com/Confidenceman02/djelm
*/ var parcelHelpers = require("@parcel/transformer-js/src/esmodule-helpers.js");
var _defo = require("@icelab/defo");
var _defoDefault = parcelHelpers.interopDefault(_defo);
const views = {
    djelmMain: async (el, data)=>{
        //@ts-ignore
        const { Elm } = await require("b9a8fedcf3c13dd0");
        const app = Elm.Main.init({
            node: el,
            flags: data
        });
        return {
            // Called whenever the value of the `djelm` attribute changes
            update: (newData, oldData)=>{},
            // Called when the element (or its djelm attribute) is removed from the DOM
            destroy: ()=>{}
        };
    }
};
(0, _defoDefault.default)({
    views,
    prefix: "elmprogramsmain"
});

},{"@icelab/defo":"lfEr5","b9a8fedcf3c13dd0":"ium5z","@parcel/transformer-js/src/esmodule-helpers.js":"6elpC"}],"lfEr5":[function(require,module,exports) {
!function(e, t) {
    module.exports = t();
}(this, function() {
    "use strict";
    function e(e, t) {
        return function(e, t) {
            return Object.keys(e.dataset).filter((e)=>0 === e.indexOf(t));
        }(e, t).length > 0;
    }
    function t(e) {
        return e.charAt(0).toUpperCase() + e.slice(1);
    }
    function r(e) {
        try {
            return JSON.parse(e);
        } catch (e) {}
        return e;
    }
    function o({ prefix: e, scope: t, views: r }) {
        Object.keys(r).forEach((o)=>{
            const a = `data-${e}-${(s = o, s.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase())}`;
            var s;
            let i = Array.prototype.slice.call(t.querySelectorAll(`[${a}]`));
            t.hasAttribute(a) && (i = [
                t
            ].concat(i)), i.forEach((t)=>{
                n(t, e, r, o);
            });
        });
    }
    function n(e, o, n, a) {
        if (e._defoUpdate && e._defoUpdate[a]) return;
        const s = n[a], i = function(e, r) {
            var o;
            return `${o = e, o.replace(/^[_.\- ]+/, "").toLowerCase().replace(/[_.\- ]+(\w|$)/g, (e, t)=>t.toUpperCase()).replace(/\d+(\w|$)/g, (e)=>e.toUpperCase())}${t(r)}`;
        }(o, a);
        if (!s || !(i in e.dataset)) return;
        const c = s(e, r(e.dataset[i]));
        e._defoUpdate = e._defoUpdate || {}, e._defoDestroy = e._defoDestroy || {}, e._defoUpdate[a] = function(e) {
            return function(t, o) {
                t = t ? r(t) : t, o = o ? r(o) : o, Promise.resolve(e).then((e)=>{
                    e.update && e.update(t, o);
                });
            };
        }(c), e._defoDestroy[a] = function(e, t, r) {
            return function() {
                Promise.resolve(e).then((e)=>{
                    e.destroy && (e.destroy(), delete t._defoUpdate[r], delete t._defoDestroy[r]);
                });
            };
        }(c, e, a);
    }
    function a({ prefix: r, scope: a, views: s }) {
        const i = new MutationObserver((a)=>{
            a.forEach((a)=>{
                const i = a.target;
                if ("attributes" === a.type && function(e, t) {
                    return 0 === e.indexOf(`data-${t}-`);
                }(a.attributeName, r)) {
                    const e = a.attributeName.split("-").slice(2).map((e, r)=>r > 0 ? t(e) : e).join("");
                    i.hasAttribute(a.attributeName) ? null !== a.oldValue ? i._defoUpdate[e](i.getAttribute(a.attributeName), a.oldValue) : n(i, r, s, e) : i._defoDestroy[e]();
                } else "childList" === a.type && (Array.prototype.slice.call(a.removedNodes).filter((e)=>e.nodeType === e.ELEMENT_NODE).filter((t)=>e(t, r)).forEach((e)=>{
                    Object.keys(e._defoDestroy).forEach((t)=>{
                        e._defoDestroy[t]();
                    });
                }), Array.prototype.slice.call(a.addedNodes).filter((e)=>e.nodeType === e.ELEMENT_NODE).forEach((e)=>{
                    Promise.resolve(e).then((e)=>{
                        o({
                            prefix: r,
                            scope: e,
                            views: s
                        });
                    });
                }));
            });
        });
        return i.observe(a, {
            attributes: !0,
            attributeOldValue: !0,
            childList: !0,
            characterData: !1,
            subtree: !0
        }), o({
            prefix: r,
            scope: a,
            views: s
        }), i;
    }
    return function({ prefix: e = "defo", scope: t = document.documentElement, views: r = {} } = {}) {
        const o = a({
            prefix: e,
            scope: t,
            views: r
        });
        return {
            destroy: ()=>{
                o.disconnect();
            }
        };
    };
});

},{}],"ium5z":[function(require,module,exports) {
module.exports = require("86b3721464c52f54")(require("b52510dfd5a0f708").getBundleURL("g2f4l") + "Main.26f7aba8.js").catch((err)=>{
    delete module.bundle.cache[module.id];
    throw err;
}).then(()=>module.bundle.root("4XkAe"));

},{"86b3721464c52f54":"5TqJq","b52510dfd5a0f708":"bKRuN"}],"5TqJq":[function(require,module,exports) {
"use strict";
var cacheLoader = require("885ec3d2ee73a000");
module.exports = cacheLoader(function(bundle) {
    return new Promise(function(resolve, reject) {
        // Don't insert the same script twice (e.g. if it was already in the HTML)
        var existingScripts = document.getElementsByTagName("script");
        if ([].concat(existingScripts).some(function isCurrentBundle(script) {
            return script.src === bundle;
        })) {
            resolve();
            return;
        }
        var preloadLink = document.createElement("link");
        preloadLink.href = bundle;
        preloadLink.rel = "preload";
        preloadLink.as = "script";
        document.head.appendChild(preloadLink);
        var script = document.createElement("script");
        script.async = true;
        script.type = "text/javascript";
        script.src = bundle;
        script.onerror = function(e) {
            var error = new TypeError("Failed to fetch dynamically imported module: ".concat(bundle, ". Error: ").concat(e.message));
            script.onerror = script.onload = null;
            script.remove();
            reject(error);
        };
        script.onload = function() {
            script.onerror = script.onload = null;
            resolve();
        };
        document.getElementsByTagName("head")[0].appendChild(script);
    });
});

},{"885ec3d2ee73a000":"4jfoQ"}],"4jfoQ":[function(require,module,exports) {
"use strict";
var cachedBundles = {};
var cachedPreloads = {};
var cachedPrefetches = {};
function getCache(type) {
    switch(type){
        case "preload":
            return cachedPreloads;
        case "prefetch":
            return cachedPrefetches;
        default:
            return cachedBundles;
    }
}
module.exports = function(loader, type) {
    return function(bundle) {
        var cache = getCache(type);
        if (cache[bundle]) return cache[bundle];
        return cache[bundle] = loader.apply(null, arguments).catch(function(e) {
            delete cache[bundle];
            throw e;
        });
    };
};

},{}],"bKRuN":[function(require,module,exports) {
"use strict";
var bundleURL = {};
function getBundleURLCached(id) {
    var value = bundleURL[id];
    if (!value) {
        value = getBundleURL();
        bundleURL[id] = value;
    }
    return value;
}
function getBundleURL() {
    try {
        throw new Error();
    } catch (err) {
        var matches = ("" + err.stack).match(/(https?|file|ftp|(chrome|moz|safari-web)-extension):\/\/[^)\n]+/g);
        if (matches) // The first two stack frames will be this function and getBundleURLCached.
        // Use the 3rd one, which will be a runtime in the original bundle.
        return getBaseURL(matches[2]);
    }
    return "/";
}
function getBaseURL(url) {
    return ("" + url).replace(/^((?:https?|file|ftp|(chrome|moz|safari-web)-extension):\/\/.+)\/[^/]+$/, "$1") + "/";
}
// TODO: Replace uses with `new URL(url).origin` when ie11 is no longer supported.
function getOrigin(url) {
    var matches = ("" + url).match(/(https?|file|ftp|(chrome|moz|safari-web)-extension):\/\/[^/]+/);
    if (!matches) throw new Error("Origin not found");
    return matches[0];
}
exports.getBundleURL = getBundleURLCached;
exports.getBaseURL = getBaseURL;
exports.getOrigin = getOrigin;

},{}],"6elpC":[function(require,module,exports) {
exports.interopDefault = function(a) {
    return a && a.__esModule ? a : {
        default: a
    };
};
exports.defineInteropFlag = function(a) {
    Object.defineProperty(a, "__esModule", {
        value: true
    });
};
exports.exportAll = function(source, dest) {
    Object.keys(source).forEach(function(key) {
        if (key === "default" || key === "__esModule" || Object.prototype.hasOwnProperty.call(dest, key)) return;
        Object.defineProperty(dest, key, {
            enumerable: true,
            get: function() {
                return source[key];
            }
        });
    });
    return dest;
};
exports.export = function(dest, destName, get) {
    Object.defineProperty(dest, destName, {
        enumerable: true,
        get: get
    });
};

},{}]},["i44Yi"], "i44Yi", "parcelRequire6d93")

//# sourceMappingURL=Main.js.map
